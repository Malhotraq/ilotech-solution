# deps
node_modules/
.next/
out/

# db lokal (jangan di-commit ke git, tapi di-mount via Docker volume)
data/*.db
data/*.db-journal
data/*.db-shm
data/*.db-wal
*.db

# foto upload pelanggan (ikut volume Docker, jangan di-commit)
data/uploads/*
!data/uploads/.gitkeep

# hasil backup lokal (jangan di-commit, simpan di tempat aman)
backups/

# env
.env
.env.local

# OS
.DS_Store
Thumbs.db
